{
  "name": "readingattendance",
  "version": "1.0.0",
  "description": "Reading group attendance app — Firestore security rules tests",
  "private": true,
  "scripts": {
    "test": "node --test",
    "test:rules": "firebase emulators:exec --only firestore --project demo-readingattendance \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.1",
    "firebase-tools": "^13.23.0",
    "playwright": "^1.62.0"
  }
}
